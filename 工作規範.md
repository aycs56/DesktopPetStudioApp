# 官方網站工作規範

## 專案邊界

- 官方網站唯一工作區：`G:\Desktop idea\DesktopPetStudioApp-site-publish`
- Git 分支：`main`
- Git remote：`origin = https://github.com/aycs56/DesktopPetStudioApp.git`
- 本工作區只包含官方網站 HTML、網站素材與部署設定；不可加入 Qt 桌寵程式、`dist/` 驗測包、本機設定檔、Steam 產物或任何 OAuth 憑證。
- `G:\Desktop idea\official-site` 是已淘汰的錯誤路徑，禁止重新建立或作為發布來源。

## 網站內容更新流程

1. 先確認要修改的頁面、可見文案、圖片／動畫素材與語系範圍；內容必須直白、可驗證，且不得描述尚未完成的功能。
2. 同步檢查導覽列、首頁導流按鈕、對應頁面、下載連結與 SEO 欄位是否仍一致；多語系內容需避免同頁重複顯示不同語言。
3. 若新增或替換桌寵素材，只複製明確選定、可發布的網站資產；不可把桌寵專案的整個資料夾、`.git`、建置產物或使用者資料帶入網站 repo。
4. 以桌機與手機尺寸檢查文字換行、圖片裁切、錨點位置、浮動按鈕與頁腳間距；確認互動與可見文案在 Chrome、Edge、Safari 的常見瀏覽情境可正常使用。
5. 完成後才在此工作區提交與推送；推送前必須確認 Git 根目錄、`main` 分支與 `origin` URL 正確。

## CSS 快取鍵規範

- 網站共用樣式為 `assets/css/site.css`，所有公開 HTML 頁面以 `<link rel="stylesheet" href="assets/css/site.css?v=YYYYMMDD-NN">` 載入。
- **每次實質修改 `assets/css/site.css` 時，必須在同一個提交中更新 CSS 快取鍵。** 不可沿用既有值、不可只更新單一頁面，也不可在沒有 CSS 變更時任意改動快取鍵。
- 快取鍵格式固定為 `YYYYMMDD-NN`：日期使用修改當日，`NN` 為當日兩位數遞增序號。例如 `20260826-01`、`20260826-02`。
- 新快取鍵必須同步套用到所有公開 HTML：`index.html`、`privacy.html`、`terms.html`、`support.html`、`creator-json.html`、`make-your-pet.html`、`streamer-mode.html`、`404.html`，以及日後新增的公開頁面。
- CSS 驗證時，執行 `rg -n "site\\.css\\?v=" -g "*.html" .`，確認所有頁面僅使用本次的新鍵值；再以無痕視窗或硬重新整理確認部署後樣式已更新。
- 若 JavaScript 有實質變更，應依同樣原則更新 `assets/js/site.js?v=...`；這不取代 CSS 快取鍵的更新義務。

## Git 與發布前檢查

1. 在本工作區執行 `git rev-parse --show-toplevel`，結果必須是 `G:/Desktop idea/DesktopPetStudioApp-site-publish`。
2. 執行 `git branch --show-current`，結果必須是 `main`。
3. 執行 `git remote get-url origin`，結果必須是 `https://github.com/aycs56/DesktopPetStudioApp.git`。
4. 檢查 `git status --short`，只提交本次網站變更；不得使用跨工作區的萬用字元或遞迴加入。
5. 推送前完成 HTML／CSS 靜態檢查與主要桌機、手機畫面驗證；部署後再次確認公開網址載入最新 CSS 快取鍵。

## 維護紀錄

- 2026-08-26：建立官方網站專用工作規範；新增網站內容更新檢查與 CSS 快取鍵必須同步遞增的發布規則。
